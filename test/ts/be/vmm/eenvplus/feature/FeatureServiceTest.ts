module be.vmm.eenvplus.feature {
    'use strict';

    beforeEach(module('ep_feature'));

    describe('FeatureService', () => {

        var service;

        //beforeEach(inject(function(_epFeatureService_) {
        //    service = _epFeatureService_;
        //    console.log(service);
        //}));

        it('should fail', () => {
            expect(true).to.be.true();
        });

    });

}
